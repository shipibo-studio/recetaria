import { AppRouteLoading } from "./components/route-loading"

export default function Loading() {
  return (
    <AppRouteLoading
      title="Cargando vista"
      description="Estamos preparando tus datos para que no veas una pantalla vacía."
    />
  )
}
