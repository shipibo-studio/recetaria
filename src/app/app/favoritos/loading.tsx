import { AppRouteLoading } from "../components/route-loading"

export default function Loading() {
  return (
    <AppRouteLoading
      title="Cargando favoritos"
      description="Buscando tus recetas guardadas."
    />
  )
}
