import { AppRouteLoading } from "../../components/route-loading"

export default function Loading() {
  return (
    <AppRouteLoading
      title="Cargando receta"
      description="Preparando el detalle de la receta seleccionada."
    />
  )
}
